# Development proxy target (local Laravel or ngrok).
VITE_PROXY_TARGET=http://100.94.240.111:8000
# Browser requests stay on the frontend origin during development.
VITE_BACKEND_ORIGIN=
VITE_API_BASE_URL=/api/v1
